# Example environment configuration for Quick Air Chat Bot
# Duplicate this file to `.env` and fill in the required secrets.

# Required: Gemini API key (obtain from Google AI Studio / MakerSuite)
GEMINI_KEY=

# Model identifier (ensure endpoints support this model)
MODEL=gemini-2.0-flash-001

# Server port (optional)
PORT=9090

# Support email recipient (optional)
SUPPORT_TO=

# SMTP configuration (optional - required for sending emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Notes:
# - Never commit real secret values.
# - Rotate keys immediately if accidentally exposed.
# - Keep this example file updated when adding/removing environment variables.
